@PACKAGE_INIT@

include(CMakeFindDependencyMacro) # This is a CMake module that provides the find_dependency() function.

@PACKAGE_DEPENDENCIES@

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@.cmake") # This is the file that contains the targets for the library.
set(@PROJECT_NAME@_VERSION "@PROJECT_VERSION@")

